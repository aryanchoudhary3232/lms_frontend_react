// Centralized middleware exports for React frontend
import ProtectedRoute from "./ProtectedRoute";

// Re-export all middleware components
export { ProtectedRoute };

// Default export for convenience
export default {
  ProtectedRoute,
};
